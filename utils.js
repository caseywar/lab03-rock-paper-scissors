export function makeRockPaperSissors(throwOptions) {
    if (throwOptions === 1) {
        return 'rock';
    }
    if (throwOptions === 2) {
        return 'paper';
    }
    if (throwOptions === 3) {
        return 'sissors'
    }
}